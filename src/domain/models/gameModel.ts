export type GameModel = {

}

export type GameMinModel = {
    id: number,
    title: string,
    score: number,
    imgUrl: string,
    position: number,
};

export type StarRatingModel = {
    score: number
}